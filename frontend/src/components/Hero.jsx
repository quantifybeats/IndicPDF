import React from 'react';

const Hero = () => {
  return (
    <section className="hero py-16 px-6 text-center bg-surface">
      <h1 className="text-[clamp(2rem,5vw,3rem)] font-extrabold mb-4 text-[#222]">
        Every tool you need to work with PDFs in one place
      </h1>
      <p className="text-lg text-text-muted max-w-[800px] mx-auto">
        Securely convert, analyse, and process Indian language documents (Telugu, Hindi, Tamil) with enterprise-grade HarfBuzz shaping. 100% secure. Files deleted in 2 hours.
      </p>
    </section>
  );
};

export default Hero;
